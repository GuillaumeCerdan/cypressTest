name: Node CI

on: push

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@main

      - name: Install 🔧
        run: npm install

      - name: Build 🏗️
        run: npm run build

      - name: Start 👌🏻
        run: npm start

      - name: Cypress 👺
        run: npm cypress:open
